@page "/regions"
@inject IListService<Region> RegionService;
@inject NavigationManager Navigation;

<DryPageQueryView>
    <TableMenu TItem="Region" />
    <DryTable TItem="Region" ViewModel="@this" ItemsService="@RegionService" />
</DryPageQueryView>

@if (CompanyUuid != Guid.Empty)
{
    <CompanyEditDialog Uuid="@CompanyUuid" />
}

@code {

    [Command(CommandContext.Alternate, Icon = "edit")]
    public void Edit(Region region)
    {
        Console.WriteLine($"Huzzah {region.Code}");
        //CompanyUuid = company.Uuid;
        StateHasChanged();
        //Navigation.NavigateTo($"/companies/{company.Uuid}");
    }

    public Guid CompanyUuid { get; set; } = Guid.Empty;

}
