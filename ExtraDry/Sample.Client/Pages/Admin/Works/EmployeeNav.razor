@page "/admin/works/employees/{View}"
@page "/admin/works/employees/{View}/{Uuid:guid}"

@inject NavigationManager Navigation;

@if(View == "list") {
    <EmployeeList />
}
@if(Uuid != Guid.Empty)
{
    <DialogBox CloseUrl="@CloseDialogUrl">
        <DryErrorBoundary>
            <EmployeeEdit Uuid="@Uuid" />
        </DryErrorBoundary>
    </DialogBox>
}

@code {

    [Parameter]
    public string View { get; set; } = string.Empty;

    [Parameter]
    public Guid Uuid { get; set; } = Guid.Empty;

    private string CloseDialogUrl => $"/admin/works/companies/{View}";

}
