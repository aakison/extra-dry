@page "/admin/works/sectors/{View}"
@page "/admin/works/sectors/{View}/{Uuid:guid}"

@inject NavigationManager Navigation;

@if(View == "list") {
    <SectorList />
}
@if(Uuid != Guid.Empty)
{
    <DialogBox CloseUrl="@CloseDialogUrl">
        <DryErrorBoundary>
            <SectorEdit Uuid="@Uuid" />
        </DryErrorBoundary>
    </DialogBox>
}

@code {

    [Parameter]
    public string View { get; set; } = string.Empty;

    [Parameter]
    public Guid Uuid { get; set; } = Guid.Empty;

    private string CloseDialogUrl => $"/admin/works/sectors/{View}";

}
