@namespace ExtraDry.Blazor
@typeparam TItem

<div class="mini-card @SemanticThumbnail @SemanticSubtitle">
    @if(ShowThumbnail) {
        <img class="thumbnail" src="@Thumbnail" />
    }
    <div class="titles">
        <span class="title">@Title</span>
        @if(ShowSubtitle) {
            <span class="subtitle">@Subtitle</span>
        }
    </div>
</div>
